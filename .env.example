# Copie para .env e ajuste o valor (desenvolvimento local).
# Não commite o .env (já está no .gitignore).

VITE_API_BASE_URL=http://localhost:8080

# Produção (Docker/Railway): defina API_BASE_URL na variável de ambiente do container.
# O entrypoint gera public/env.js em runtime; não é necessário rebuild.
