# Desenvolvimento local: copie para .env (não commite)
VITE_API_BASE_URL=http://localhost:8080

# Google Sign-In (opcional): Client ID do projeto no Google Cloud Console
# VITE_GOOGLE_CLIENT_ID=xxx.apps.googleusercontent.com

# Produção (Railway): defina VITE_API_BASE_URL e VITE_GOOGLE_CLIENT_ID nas variáveis do serviço do frontend.
# (O app adiciona /api automaticamente à URL.)
